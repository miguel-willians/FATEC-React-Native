import { StyleSheet } from "react-native";

export default StyleSheet.create({
  resultado: {
    fontSize: 18,
    textAlign: "center",
    marginTop: 20,
    color: "#333",
    fontWeight: "600",
  },
});