import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  resultado: {
    marginTop: 20,
    padding: 15,
    borderWidth: 1,
    borderColor: "#ccc",
    borderRadius: 5,
  },
  subtitulo: {
    fontSize: 18,
    fontWeight: "bold",
    color: "blue",
    marginBottom: 10,
  },
});

