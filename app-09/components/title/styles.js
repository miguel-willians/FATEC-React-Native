import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 26,
    fontWeight: 'bold',
    color: '#FF5959',
    marginBottom: 20,
    textAlign: 'center',
  },
});