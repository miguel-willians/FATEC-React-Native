import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  screenTitle: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#333',
    marginBottom: 10,
  },
  screenText: {
    fontSize: 16,
    color: '#666',
    textAlign: 'center',
  },

});

export default styles;